---
import Mermaid from './Mermaid';
import type { MermaidProps } from './Mermaid';

export interface Props extends MermaidProps {}

const props = Astro.props;
---
<Mermaid {...props} client:only='react' />